interface:
  display_name: "OS Agent Task"
  short_description: "核心目标导向的轻量工程工作流"
  default_prompt: "Use $os-agent-task for one bounded task defined by objective value, key boundaries, minimal acceptance, and stop conditions. Default to direct implementation plus directly related build or tests; escalate only when the skill criteria require it."
policy:
  allow_implicit_invocation: false
