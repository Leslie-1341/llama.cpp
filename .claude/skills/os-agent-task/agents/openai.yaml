interface:
  display_name: "OS Agent Task"
  short_description: "系统级根因、受控实现、对抗审查、实验与工程账本工作流"
  default_prompt: "Use $os-agent-task with mode=contract|audit|implement|review|review-fix|script|memory, then provide a concise goal and acceptance criteria. For high-risk stateful changes, establish invariants and lifecycle before editing."
policy:
  allow_implicit_invocation: false
