# review-fix

目标：只修复已由当前源码或原始 artifact 证明的阻塞项。

前置必须具备：真实可达调用链、首错和根因、不变量、最小修改范围、覆盖原失败的测试。任一缺失则不修改，输出“证据不足，退回 audit”。

阻塞项准入：源码可达、属于当前阶段、会产生可观察错误或假通过。禁止把代理遗漏、未覆盖字段、已知限制或无新证据的重复症状升级为阻塞。

只修确认项，不重做整个审计、不扩大功能、不顺手处理非阻塞优化。运行定向测试和 review-fix Gate。

输出按“阻塞项 → 根因/不变量 → 修改 → 短验证 → 尚缺证据”压缩汇报，并说明是否可进入下一验证。
