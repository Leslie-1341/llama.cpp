IMPORTANT: Ensure you’ve thoroughly reviewed the [AGENTS.md](AGENTS.md) file before beginning any work.
# OS Competition Project Instructions

## 1. 项目背景

本仓库基于 llama.cpp，用于全国大学生操作系统大赛功能赛道决赛作品。

当前分工：

- 用户主要负责 KV Cache 管理与优化。
- 队友主要负责权重管理和专家激活优化。
- 后续需要完成 KV Cache、权重管理、专家调度之间的融合。
- 决赛重点不是继续堆叠功能，而是提高创新性、工程完成度、性能、稳定性、融合效果、演示质量和答辩可信度。
- 项目当前状态会持续变化，任何结论都必须以当前分支代码、README、docs、测试结果和 git diff 为准。

不得将计划、理论推测或历史文档描述成当前已实现功能。

## 2. 回答和工作原则

回复使用中文，先给结论，减少寒暄和重复描述。

必须明确区分：

- 已实现并经过验证；
- 已实现但证据不足；
- 尚未实现；
- 仅供讨论的方案。

没有代码、运行输出或实验数据支撑时，必须明确写“目前无法确认”，并说明缺少什么证据。

不得：

- 编造性能收益、测试结果、代码行为或创新点；
- 用理论分析代替真实实验；
- 为了看起来完成而增加低价值功能；
- 将多个已有功能简单组合后称为创新；
- 隐藏失败测试、性能退化或兼容性问题。

## 3. 任务优先级

按以下优先级处理问题：

- P0：正确性、崩溃、数据损坏、内存错误、并发错误、构建失败、演示失败。
- P1：性能、创新性、模块融合、实验可信度、评委可能质疑的问题。
- P2：代码整理、体验、表达、非关键功能。

发现 P0 问题时，不应继续进行低优先级优化。

## 4. 修改源码前的强制检查

修改源码前：

1. 阅读与任务直接相关的 README、docs、测试和历史记录。
2. 先定位真实调用链和数据流，再提出修改方案。
3. 优先做范围明确、可验证、可回滚的修改。

不得覆盖不属于当前任务的已有改动。

不得主动执行：

- `git commit`
- `git push`
- merge、rebase、tag 或 release
- `git reset --hard`
- `git clean -fd`

Git 提交、推送、合并和发布由用户本人完成。

## 5. CodeGraph 使用规则

CodeGraph 当前以 MCP 工具形式接入。

适合使用 CodeGraph 的场景：

- 初次理解陌生模块；
- 分析跨文件调用链；
- 定位某个结构体、函数或状态的使用范围；
- 分析 KV Cache 分配、回收、调度和推理调用路径；
- 修改公共接口、核心数据结构或调度逻辑前评估影响范围；
- 分析 KV Cache 与权重管理、专家激活模块之间的耦合关系。

推荐调用顺序：

1. 索引状态不明确时调用 `codegraph_status`。
2. 使用 `codegraph_search` 定位目标符号或模块。
3. 使用 `codegraph_context` 或 `codegraph_node` 获取局部架构。
4. 使用 `codegraph_callers` 和 `codegraph_callees` 确认调用链。
5. 修改公共接口或核心结构前使用 `codegraph_impact`。

CodeGraph 不应机械调用：

- 单文件的小范围修改；
- 已明确知道文件和函数位置；
- 只修改注释、文档或简单测试；
- CodeGraph 索引已经过期或状态异常。

CodeGraph 结果只能用于定位和建立初步架构，最终结论必须直接读取关键源码验证。若 CodeGraph 与当前源码冲突，以当前源码为准。

## 6. KV Cache 优化审查要求

分析每项 KV Cache 优化时，必须说明：

- 解决的具体问题；
- 当前代码中的集成位置；
- 工作机制；
- 预期收益及其依据；
- CPU、内存、显存和时延代价；
- 并发安全和生命周期风险；
- 可能退化或失效的场景；
- 测试方法；
- 可量化验收标准。

重点检查：

- KV Cache 内存占用和峰值占用；
- 内存碎片和连续内存需求；
- 分配、释放、复用和淘汰策略；
- 数据布局和访存局部性；
- Prefix Cache 命中率和错误复用风险；
- 分页管理和页表开销；
- 量化或压缩产生的精度损失；
- 并发请求下的隔离、竞争和调度；
- 长上下文下的吞吐和延迟；
- 首 Token 延迟 TTFT；
- 单 Token 延迟 TPOT；
- 吞吐量、缓存命中率和显存之间的权衡。

涉及压缩、量化或近似计算时，必须增加正确性或精度对照实验。

## 7. 模块融合要求

不能只分别证明 KV Cache 和权重管理有效，还必须检查组合后的真实效果。

融合分析至少覆盖：

- 内存、显存和 CPU 资源竞争；
- KV 调度与权重加载、专家激活的时序冲突；
- 并发请求下的优先级和反压；
- 预取是否造成带宽争用；
- 独立优化有效但组合后退化的情况；
- 错误恢复和降级路径；
- 模块之间是否存在重复缓存或重复预测。

融合结果必须同时提供：

- 原始基线；
- 仅启用 KV 优化；
- 仅启用权重或专家优化；
- 同时启用全部优化；
- 必要的消融实验。

## 8. 性能实验规范

性能结论必须包含：

- 基线版本；
- 当前提交或工作树状态；
- 编译器、编译参数和构建类型；
- CPU、GPU、内存和操作系统环境；
- 模型名称、量化类型和模型文件；
- 输入长度、输出长度、批大小和并发数；
- 是否预热；
- 重复次数；
- 平均值以及波动范围；
- 指标的明确计算方式。

不得使用单次最好结果作为最终结论。

至少记录：

- 吞吐量；
- TTFT；
- TPOT；
- 峰值内存或显存；
- KV Cache 实际占用；
- 缓存命中率；
- 长上下文性能；
- 并发稳定性；
- 正确性或精度变化。

测试失败、异常值和退化结果不得静默删除。

## 9. 服务器安全规则

当前环境以 root 用户运行，所有命令都可能直接影响服务器。

未经用户明确允许，不得：

- 修改系统级网络、SSH、代理或认证配置；
- 安装或升级系统软件包；
- 删除仓库外文件；
- 删除已有构建目录或测试数据；
- 杀死不属于当前任务的进程；
- 修改其他用户文件；
- 输出 Token、密码、密钥或认证文件内容。

不要修改 `/root/.claude/settings.json`。

## 10. 构建和验证

不要猜测构建命令。先从 README、CMake 配置、脚本和现有 build 目录确认项目实际构建方式。

源码修改后最好给出与改动范围匹配的验证命令由用户执行：

- 构建检查；
- 相关单元测试或 smoke test；
- 必要的功能测试；
- 涉及性能路径时进行小规模性能回归；
- 涉及并发和内存时检查崩溃、泄漏、越界和竞争风险。

无法执行完整测试时，明确说明未验证范围和原因。

## 11. 架构图与文档

Archify Skill 已安装，在需要架构图、调用流程图、数据流图或生命周期图时可以调用。

使用顺序：

1. 先通过 CodeGraph 和源码确认真实结构。
2. 再生成图。
3. 图中区分已实现模块、计划模块和外部依赖。
4. 不得为了视觉完整而虚构组件或数据流。
5. 架构图用于表达证据，不能替代代码和实验依据。

## 12. 输出格式

完成分析或修改后，优先按以下结构输出：

1. 结论；
2. 证据或定位；
3. 修改内容；
4. 验证结果；
5. 剩余风险；
6. 最关键的下一步。

涉及源码时，指出具体文件、函数、结构体或调用链。

涉及实验时，明确说明基线、变量、指标和重复次数。